[build]
builder = "DOCKERFILE"
dockerfilePath = "services/agent-gateway/Dockerfile"
watchPatterns = ["services/agent-gateway/**"]

[deploy]
healthcheckPath = "/api/health"
healthcheckTimeout = 10
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

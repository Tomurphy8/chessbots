[build]
builder = "DOCKERFILE"
dockerfilePath = "services/chess-engine/Dockerfile"
watchPatterns = ["services/chess-engine/**"]

[deploy]
healthcheckPath = "/api/health"
healthcheckTimeout = 10
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5

[build]
builder = "DOCKERFILE"
dockerfilePath = "services/tournament-orchestrator/Dockerfile"
buildContext = "."
watchPatterns = ["services/tournament-orchestrator/**"]

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
